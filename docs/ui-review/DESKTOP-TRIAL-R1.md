# 本地桌面试用验收 · 2026-09-06

附件说明：验收截图、本机日志及统计快照仅保留在本地，不随源码提交；下文保留验收结论和场景说明。

## 范围与交付状态

沿用 feat/codex-style-ui、HEAD c00e9b7f2b85985423dbcc7919b7058bc8218010 及全部未提交 UI 成果。本轮没有新增生产代码修改，没有修改版本、appId、包名、用户数据路径、依赖或业务层。新增本记录及 7 张真实 Electron 截图，使用现有流程生成本地安装包。没有 commit、push、合并或公开发布。

这是可启动、已完成真实模型基本闭环的本地试用包；不是全部验收通过的发布候选版本。中断输出恢复、原生窗口细项和指定尺寸仍存在验收缺口。

## 参考与审查

沿用 README.md 中 2026-09-06 已确认的同一套独立 Codex 桌面参考。本轮使用 Product Design audit 流程检查实际客户端，没有增加风格探索、网页入口或展示产品。没有获得同版本本机 Codex 的额外参考截图；不宣称像素级一致。原有 renderer 是本仓库唯一生产 UI 入口，ui-preview 为既有独立测试入口，不进入安装包。

## 包与安装验证

平台：macOS arm64。产品：Calmnova Code。版本：1.0.3。appId：com.calmnova.code。

安装包（相对 app 仓库根目录）：

- packages/app/release/ui-trial-20260906/Calmnova-Code-1.0.3-arm64.dmg
- packages/app/release/ui-trial-20260906/Calmnova-Code-1.0.3-arm64.zip

DMG SHA-256：0043bf3409d52397ceafab692bd48c524853b14f6847b860abcfbfea9d131cd8

ZIP SHA-256：306e9591a027f3beb8f6218f93ae36c5884330860cc23ee7354ab85d1701fca0

使用现有 electron-builder.yml，显式 --publish never，独立输出目录避免覆盖旧包。使用已安装 Electron 40.10.0 和经过 manifest 校验的 Codex 0.153.2，没有升级依赖。签名为 ad-hoc，未公证。

hdiutil verify 校验通过；只读挂载 DMG 后，将其中应用复制到 release/ui-trial-20260906/installed/Calmnova Code.app，codesign --verify --deep --strict 通过。实际验收启动的是该安装副本；其窗口地址确认为 app.asar/dist/renderer/index.html，不是开发服务器或浏览器预览。未覆盖系统已有安装。

初次连接构建目录内应用曾多次超时，确认进程存在后结束本轮该进程。从 DMG 安装副本启动和退出后重启均成功；没有诊断凭据存储或修改授权机制。

## 实际桌面验收

| 项目                  | 结果与证据                                                                                         |
| --------------------- | -------------------------------------------------------------------------------------------------- |
| 启动安装副本          | 已验证，真实 Electron 窗口                                                                         |
| 模型选择              | 已验证，使用已有 gpt-5.5，临时选择低推理；结束后恢复极高；没有新增凭据或测试其他服务商             |
| 项目选择              | 已验证，系统目录选择器选择本轮创建的 /tmp/calmnova-trial-project，仅含公开测试 README              |
| 新会话、输入、发送    | 已验证，中文粘贴输入，发送按钮由禁用变为可用，真实发送两次                                         |
| 模型回复              | 已验证，第一次按请求完成中文一至三十                                                               |
| 流式输出              | 已验证，第二次观察到输出至第 43 行且显示“模型正在输出内容”                                         |
| 停止                  | 已验证，点击停止后最终停在第 91 行，未继续至请求的 300 行；停止按钮消失                            |
| 会话切换              | 已验证，新建空会话后切回本轮会话；完整回复保留                                                     |
| 重启历史              | 部分通过：重启后本轮完整回复和两条任务均可重新打开；中断部分回复未恢复，出现原始 turn_aborted 提示 |
| 主题偏好保存          | 已验证，切到深色，退出后重启仍为深色；最终恢复原来的浅色                                           |
| 设置返回、AI 模型分类 | 已验证，分类独立、原有配置能力可见，返回会话可用；未更改或重新验证服务商凭据                       |
| 设置保存覆盖面        | 只验证主题偏好持久化；没有改全局模型、沙箱或授权设置，其他表单保存未验证                           |
| 文件查看              | 已验证，文件树打开测试 README，编辑器、标签关闭可用；不宣称验证编辑保存或真实 Git Diff             |
| 最大化/还原           | 最大化已验证按钮变“还原”，截图宽度从 1200 增至 1280；完整原生还原/拖动/自由缩放未验证              |
| 最小化                | 已点击原有按钮，但自动化会重新激活窗口，未取得足够证据证明最小化状态                               |
| 中文输入法            | 未验证候选确认；中文粘贴不等于 IME 验收。源码已有 isComposing 判断，不以源码代替实机通过           |
| 1280×800 / 1440×900   | 受当前桌面捕获范围限制，实际图片为 1200×711 或 1280×711，不能声称覆盖指定窗口尺寸                  |

仅两次真实模型请求。没有读取或输出密钥，没有对其他供应商执行测试连接。应用正常启动会加载既有历史；未打开其他个人会话，保存截图前通过原有控件收起私人历史列表。截图保留真实运行状态和错误/中断提示。

## 截图（全部为 DMG 安装副本的 Electron 窗口）

- 空白页
- 模型菜单
- 文件树与编辑器
- 真实停止后的会话
- AI 模型设置
- 深色
- 重启后恢复及中断提示

图片未做内容伪造或替换；原有 README 的 before/after 图片仍为浏览器组件预览，不能拿来证明原生效果。

## 检查

- pnpm run format:check：通过。
- pnpm run lint：通过。
- pnpm run typecheck：通过，现有 6 包 tsc；不等同 vue-tsc。
- pnpm run test：58 个文件 / 440 项通过。首次受沙箱禁止回环监听影响，允许本机测试服务器后重跑通过，未改断言。
- pnpm run codex:runtime:verify：通过。
- pnpm run build：通过；保留现有构建体积提示。
- git diff --check：通过。
- DMG 构建最初受工具缓存写入权限限制，获准使用现有缓存后完成；没有发布。

## 未通过与后续

中断输出在历史重新加载后丢失：影响中断任务的可追溯性，属于试用限制，不能标历史全部通过。已完成回复恢复通过。本轮 UI diff 未修改历史服务、协议或回放逻辑，没有通过改动该层扩大范围。

历史标题重新加载后可能取到 recommended_plugins 环境文本，且中断提示原样显示 turn_aborted：影响会话识别和可读性。需后续单独修正历史展示来源与过滤规则，不能仅为外观隐藏真实中断状态。

浅色编辑器分隔槽仍有明显深色带；顶部自定义操作与 macOS 原生窗口风格未完全统一；这些不阻挡本轮发送/停止，列入视觉收尾清单。

IME、精确两档窗口尺寸、原生拖动/最小化/还原/自由缩放、完整设置保存覆盖未通过验收，不宣称不存在问题。安装包未公证，跨机器分发和 Gatekeeper 首次下载场景未验证。本轮可本机试用，不能据此公开发布。
